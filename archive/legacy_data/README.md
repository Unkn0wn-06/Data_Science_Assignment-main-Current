# Archived generated datasets

These files are retained only for historical reproducibility:

- `final_prepared_dataset.csv`: older 3,814-row, 18-column prepared dataset.
- `recleaned_prepared_dataset.csv`: superseded 3,791-row, 18-column prepared dataset.

Active code must use `data/processed/production_prepared_dataset.csv` instead.

