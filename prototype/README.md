# Streamlit prototype

The real dashboard implementation is `app.py`. It reads the canonical processed
dataset and parameter file with paths derived from the repository root.

From the project root, start it with:

```bash
streamlit run prototype/app.py
```

The root `app.py` remains only as a compatibility launcher for older deployments.

